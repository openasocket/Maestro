import type { Theme } from '../../../../types';
import type { CreatedFileInfo } from '../../services/phaseGenerator';

export interface PreparingPlanScreenProps {
	theme: Theme;
}

export type { CreatedFileInfo };
